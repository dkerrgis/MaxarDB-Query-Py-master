[pytest]
markers =
    large_area_exception: Raise exception if area too large_area_exception
    wrong_prj: Raise exception when wrong prj
filterwarnings = 
    ignore::FutureWarning
    ignore::DeprecationWarning